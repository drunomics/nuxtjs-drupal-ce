export const actions = {
  // Uncomment to enable menu fetching from the configured menu API endpoint.
  // If not in use, the file and empty folder should be deleted.
  async nuxtServerInit ({ dispatch }, context) {
    // await context.$drupal.fetchMenu('main')
  }
}
