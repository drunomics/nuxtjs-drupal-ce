{#
/**
 * @file
 * Default theme implementation for displaying an custom elements entity.
 *
 * Available variables:
 * - tag_prefix: Prefix to use for the html tag - including the prefix separator.
 * - tag: HTML tag to use for the entity.
 * - attributes: The attributes to add to the tag.
 * - slots: Fields rendered as conten via slots. An array keyed by slot name and containing another array with the
 *   entries:
 *   - 'tag': The tag to use for the slot element.
 *   - 'content': The renderable content.
 *   - 'key': The slot name/key.
 *   - 'attributes': Slot attributes to add to the tag.
 *
 * @ingroup themeable
 */
#}

<{{ tag_prefix }}{{ tag }}{{ attributes.addClass('custom-element') }}>
  {% for key,slot in slots %}
  <{{ slot.tag }}{{ slot.attributes.setAttribute('slot', key) }}>
    {{ slot.content }}
  </{{ slot.tag }}>
  {% endfor %}
</{{ tag_prefix }}{{ tag }}>
