{#
/**
 * @file
 * Default theme implementation for displaying an custom elements entity.
 *
 * Available variables:
 * - tag_prefix: Prefix to use for the html tag.
 * - tag: HTML tag to use for the entity.
 * - attributes: The attributes to add to the tag. Includes data-attributes
 *   added by the preprocessors.
 * - slots: Rendered markup fields that are not in data-attributes.
 *
 * @ingroup themeable
 */
#}

<{{ tag_prefix }}-{{ tag }} {{ attributes.addClass('custom-elements') }}>
  {% for key,value in slots %}
    <template slot="{{ key }}">{{ value }}</template>
  {% endfor %}
</{{ tag_prefix }}-{{ tag }}>
